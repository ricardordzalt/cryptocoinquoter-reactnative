import React from 'react';
import { Text } from 'react-native';
import { styles } from './styles';

const Header = () => {
    return (
        <Text style={styles.header}>Cryptocoins</Text>
    );
};

export default Header;